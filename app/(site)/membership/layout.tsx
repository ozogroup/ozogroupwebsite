import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Book Membership · OZO Skin Care Premium Referral Partner",
  description: "Join the OZO Skin Care Premium Referral Partner Program. Book your membership and start earning commissions.",
};

export default function MembershipLayout({ children }: { children: React.ReactNode }) {
  return children;
}
