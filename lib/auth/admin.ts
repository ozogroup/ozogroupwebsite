/**
 * Admin authentication module (placeholder).
 *
 * Will contain:
 *  - signInAdmin(email, password)
 *  - signOutAdmin()
 *  - getCurrentAdmin()  (server-side)
 *  - requireAdmin()      (middleware/guard helper)
 *
 * Implementation pending creation of `admins` table & RLS policies.
 */

export const ADMIN_AUTH_PLACEHOLDER = true;
