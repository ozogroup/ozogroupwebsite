/**
 * Partner authentication module (placeholder).
 *
 * Will contain:
 *  - signUpPartner(payload)
 *  - signInPartner(email, password)
 *  - signOutPartner()
 *  - getCurrentPartner()
 *  - requirePartner()
 *
 * Implementation pending creation of `partners` table & RLS policies.
 */

export const PARTNER_AUTH_PLACEHOLDER = true;
